# Circles / Gnosis Chain (chain id 100) — used in later milestones
VITE_GNOSIS_CHAIN_ID=100
# Optional analytics / referral backend (e.g. Supabase) — not required for the MVP loop
VITE_ANALYTICS_URL=
